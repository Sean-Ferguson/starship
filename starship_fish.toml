command_timeout = 500

[character]
  # success_symbol = "[➜](bold green)"
  success_symbol = "[🐟](bold green)"
  # success_symbol = "[→](bold green)"
  error_symbol = "[✗](bold red)"
  #vim
  vimcmd_symbol = "[N](bold blue)"                    # normal   (esc)
  vimcmd_visual_symbol = "[V](bold purple)"           # visual   (esc v)
  vimcmd_replace_symbol = "[R](bold red)"             # replace  (esc R)
  vimcmd_replace_one_symbol = "[R](bold yellow)"      # replace  (esc r)

[directory]
  truncation_length = 0
  # home_symbol = "🏠"
  home_symbol = "🏡"
  # root_symbol = "🍄"        # doesnt seem to work anymore
